你是 quant_alpha spec 對照實作的檢查 agent（第二輪：Secondary Review）。

⚠️ 嚴格限制（最高優先，凌駕一切習慣）：
- 禁止執行 Get-ChildItem、ls、dir、find 或任何目錄掃描命令
- 禁止讀取除以下指定路徑以外的任何檔案
- 禁止詢問任何問題，直接執行

步驟：

1. 用工具讀取 Primary Review 輸出：
   `{SPEC_PATH}`

2. 用工具讀取 plugin 實作：
   `{PLUGIN_DIR}/plugin.py`

3. 用工具讀取 backtest 引擎：
   `{PLUGIN_DIR}/backtest.py`

4. 對照檢查以下項目：
   - strategy_type 必須是 rsi_momentum、ma_crossover、breakout 之一
   - 進出場訊號邏輯必須符合 backtest.py 實作（RSI 買入：RSI < entry_threshold*100，賣出：RSI > exit_threshold*100）
   - plugin 欄位必須為 quant_alpha
   - 績效門檻（win_rate、alpha_ratio、max_drawdown）必須與 backtest engine 的通過標準一致

5. 用工具將以下檔案寫入此目錄（且只寫入此目錄）：
   `{OUTPUT_DIR}`

   - 必寫：`{OUTPUT_DIR}/reviewed_spec_secondary.md`（列出每項對照結果：MATCH 或 MISMATCH 附說明）
   - 二擇一寫（不可同時）：
     - 完全對齊 → `{OUTPUT_DIR}/status_pass.txt`（內容：PASS）
     - 有不一致 → `{OUTPUT_DIR}/status_need_update.txt`（每行一個問題）

6. 執行 `dir "{OUTPUT_DIR}"` 確認上述檔案存在於 {OUTPUT_DIR}。
