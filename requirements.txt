# ==============================================================================
# Agentic Research Workflow Engine — direct dependencies
# Tested against: Python 3.11, langgraph 1.1.3
# Last updated: 2026-03-19
# ==============================================================================

# ------------------------------------------------------------------------------
# LangGraph core
# ------------------------------------------------------------------------------
langgraph==1.1.3                    # StateGraph, interrupt(), Command
langgraph-checkpoint-postgres==3.0.5 # PostgresSaver (requires psycopg3)
langchain-core==1.2.20              # required by langgraph internals

# ------------------------------------------------------------------------------
# Database (psycopg3)
# ------------------------------------------------------------------------------
psycopg[binary]==3.3.3              # async-capable psycopg3 driver
psycopg-pool==3.3.0                 # connection pool (Phase 4 migration target)

# ------------------------------------------------------------------------------
# API server
# ------------------------------------------------------------------------------
fastapi==0.135.1
uvicorn[standard]==0.42.0           # [standard] adds uvloop + watchfiles
pydantic==2.12.5
httpx==0.28.1                       # Planka REST calls in notify.py + api/server.py

# ------------------------------------------------------------------------------
# MinIO — artifact and attachment storage
# ------------------------------------------------------------------------------
minio>=7.2

python-dotenv==1.2.2

# ------------------------------------------------------------------------------
# MLflow — experiment tracking (optional; used by sample plugin + any plugin
#          that calls _try_mlflow_log when MLFLOW_TRACKING_URI is set)
# ------------------------------------------------------------------------------
mlflow==2.20.2