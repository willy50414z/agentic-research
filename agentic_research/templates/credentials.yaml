# credentials.yaml — paths to external tool credentials
# This file stores paths/references ONLY, not actual keys.
# Actual keys live in ~/.agentic-research/.env (never commit that file).

backtest:
  freqtrade_config: ~/.freqtrade/config.json   # path to freqtrade config

data:
  # yfinance requires no credentials
  # For premium data sources:
  # tiingo_api_key_env: TIINGO_API_KEY
  # polygon_api_key_env: POLYGON_API_KEY

exchange:
  # binance:
  #   api_key_env: BINANCE_API_KEY
  #   secret_key_env: BINANCE_SECRET_KEY
